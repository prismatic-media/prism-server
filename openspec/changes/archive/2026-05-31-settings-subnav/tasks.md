## 1. Shell Template

- [x] 1.1 Add a `settings-subnav` CSS class `<li>` for "Server Settings" beneath the Admin `<li>` in `shell.component.ts`, linking to `/admin/settings` with `routerLinkActive="active"` and `(click)="menuOpen = false"`

## 2. Shell Styles

- [x] 2.1 Add `.subnav-item a` style in `shell.component.scss`
